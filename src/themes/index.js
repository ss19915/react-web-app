import { THEME } from '../constants';

const theme = {
    [THEME.DARK]: {
        color: 'white',
        backgroundColor: 'black',
    },
    [THEME.LIGHT]: {
        color: 'black',
        backgroundColor: 'white',
    }
}

export default theme;